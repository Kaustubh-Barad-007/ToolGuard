# ToolGuard Workspace Configuration

This directory contains ToolGuard configuration, tool definitions, and trusted baseline state.

- `config.json`: Local workspace scanning settings.
- `baseline.json`: Cryptographically signed, frozen state of authorized tools.
- `tools/`: Monitored tool definition files.
